services:
  - type: web
    name: paytm-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        sync: false
      - key: MONGODB_URL
        sync: false
      - key: FRONTEND_URL
        sync: false

